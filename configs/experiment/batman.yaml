# @package _global_
defaults:
  - /model: mlp_2d

data:
  name: batman
  dim: 2
  batch_size: 2048
  num_points: 8000

model:
  backbone: mlp
  hidden_dim: 512
  num_blocks: 4

prox_training:
  total_iters: 30000
  l1_iters: 10000

score_training:
  total_iters: 30000
